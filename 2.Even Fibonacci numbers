t = int(input().strip())  #for testcases
for a0 in range(t):
    n = int(input().strip())

    sum=0
    a=1
    b=1
    c=a+b
    while c<n:
        if c%2==0:
            sum+=c
        a,b=b,c
        c=a+b
    print(sum)
