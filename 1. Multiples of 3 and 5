t = int(input().strip())  #for testcases
for a0 in range(t):
    n = int(input().strip())
    n=n-1
    a=n//3
    b=n//5
    c=n//15
    sum_3=a*((a+1)*3)
    sum_5=b*((b+1)*5)
    sum_15=c*((c+1)*15)
    print(int((sum_3+sum_5-sum_15)//2))
